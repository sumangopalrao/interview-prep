What happens when you enter a URL?
GET vs POST
REST vs SOAP
Serlvet vs JSP
Databases SQL 
InnerJoin vs OuterJoin

########## Java #######################
Java Garbage Collection
HashTable vs HashMap
How does Spring work?
Authentication in Spring
Limitations of Spring
Annotations and their use


########### JS Concepts  ##############
DOM manipulation without JQuery
Regex(do not parse HTML with regex)
AJAX and asynchronous calls
Type Coercion, truth/falsy values
Namespaces and scoping: IIFES, hoisting
Closures
OOP concepts and protoypes
Basics of other frameworks: RX, Express, Ember

########## AngularJS ###################
Routing ui-routes and states
Service vs Factory


########### C Programming ##############
Using pointers and reference (similar to call by value/reference)
Deadlock example.
Mutex vs Semaphore

########### General ###################
Recursion not recommended for production code as it takes stack space proprtional to size.
K points closest to origin algorithm. 
- Use sorting then find the first k.
- Use Min heap.
- Use quick partition.
- Use k-d tree logn.
Enhancement: What if the input size is not constant and inifinite like a stream?
Perfect solution not possible but can close it down to a value close like 0.000001.


How to loop through a for loop a thousand times a second?
Use queries per second, thread.sleep((b-a) - 1 / number)


KMP Algorithm


Take an array and split them odd left and even right, then sort them. 


Square root of a number(float) without inbuilt functions.
